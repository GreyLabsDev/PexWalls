# Onboarding report template

Fill from disk. Leave a section as `UNVERIFIED` rather than guessing.

```markdown
# PexWalls onboarding — YYYY-MM-DD

_Generated by skill `onboard-project`. Source of truth: Gradle + Kotlin, not README._

## 1. What this is
- Product:
- Users:
- Not this: (e.g. not Compose, not multi-module)

## 2. Run locally
- JDK / AGP / Gradle (from catalog + wrapper):
- Android SDK: `compileSdk` / `minSdk` / `targetSdk`
- Open in Android Studio, set `sdk.dir` in `local.properties` (gitignored)
- Network: `pexels.api.key` in `local.properties` → `BuildConfig.PEXELS_API_KEY` (see `local.properties.example`). **Do not copy a real key into this file.**
- Commands:
  - `./gradlew :app:ktlint`
  - `./gradlew :app:testDebugUnitTest`
  - `./gradlew :app:assembleDebug`
- Known build blocker (cite file):

## 3. Map
- Gradle modules:
- Packages inside `:app`:
- DI entry:
- Navigation start + tabs:
- Photo / category nav arg keys:

## 4. Request flow (one path)
Mermaid **or** numbered steps for: Home category tap → CategoryPhotos → Photo.
Cite `navigation_graph.xml` and the Fragment `ARG_KEY` constants.

## 5. README drift
| README claims | Disk |

## 6. Hazards (no secret values)
- Auth / TLS / logging:
- Domain leaks:
- Broken API methods:
- Tests:

## 7. First tickets (do these, not Compose)
1. Title — files — done when
2. …
3. …

## 8. How to work with Bob here
Ask / **Onboarding Coach** → Plan → new chat → Agent. MCP: `.bob/mcp.json`. Evidence: `bob_sessions/` PNGs, not `internal-monologue/`. Do not `@` the hackathon PDF.
```
