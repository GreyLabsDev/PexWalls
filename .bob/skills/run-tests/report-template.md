# Test run report

```markdown
# Test gate — YYYY-MM-DD

- JDK:
- Commands:
- ktlint: pass/fail (files)
- testDebugUnitTest: pass/fail (test names)
- assembleDebug: pass/fail/skipped — if fail, is it `AppDatabaseMigrations`?
- connectedAndroidTest: skipped (default)
- Next action:
```
