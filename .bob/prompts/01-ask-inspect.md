# Prompt — Ask: inspect PexWalls

Use **Ask** mode. Do not write files.

Read `@AGENTS.md` and `@settings.gradle.kts`. Then explain:

1. What the app does (two sentences)
2. Gradle modules and why README “multi-module” is not true yet
3. UI stack (View Binding vs Data Binding vs Compose) with a file citation
4. How a photo gets from Pexels to the grid (Repository → use case → PhotoPagingUpdater)
5. README drift table
6. Security hazards without printing any token values

Do not recommend a Compose rewrite unless I ask.
