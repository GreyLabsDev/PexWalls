# Prompt — Agent: run tests

Use **Agent** mode. Activate skill `run-tests`.

Run all tests and style checks for this project. Do not run instrumented tests without a device/emulator. If assemble fails on `AppDatabaseMigrations`, state that explicitly.
