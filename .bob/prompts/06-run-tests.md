# Prompt — Agent: run tests

Use **Agent** mode. Activate skill `run-tests`.

Прогони тесты и проверки стиля этого проекта. Не запускай instrumented-тесты без эмулятора. Если assemble падает на `AppDatabaseMigrations`, явно так и напиши.
